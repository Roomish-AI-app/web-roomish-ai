{
  "name": "roomish-landing",
  "version": "1.0.0",
  "description": "A modern, responsive WordPress theme designed for the Roomish water tracking app landing page.",
  "main": "index.js",
  "scripts": {
    "build": "sass assets/scss/main.scss assets/css/style.css --style=expanded && sass assets/scss/main.scss assets/css/style.min.css --style=compressed",
    "watch": "sass --watch assets/scss/main.scss:assets/css/style.css --style=expanded",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.92.1"
  }
}
